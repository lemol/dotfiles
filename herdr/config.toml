onboarding = false
# Herdr configuration — https://herdr.dev
# Managed in dotfiles

[theme]
name = "nord"

auto_switch = false
[keys]
# Prefix key to enter prefix mode (mapped to ctrl+a)
prefix = "ctrl+a"
